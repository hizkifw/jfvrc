Implement the backend and packaging described in docs/architecture.md. You are the lead implementation agent, DeepSeek V4.1 Flash via Fireworks. Own only backend/shared files, root package/build configuration, tests/server, Docker/env/README. Another agent owns frontend files listed in architecture. Root scripts should support client+server dev, typecheck, build, test, start; install React/Vite client dependencies as well to avoid concurrent package edits. Use the architecture as contract, make competent implementation choices, consult primary Jellyfin code as needed. Build a complete usable implementation and meaningful mock integration tests. Do not contact real Jellyfin, deploy, or commit. Run your checks and fix failures within your ownership. Write a concise handoff report to .tasks/backend-report.md with changes, commands/results and remaining limits. Frontend may not exist immediately; coordinate by filesystem without modifying its files. All tools and reversible workspace work are authorized.
